claude.txt
shortcuts.txt
todo list.txt

# Node.js
node_modules/
npm-debug.log*

# Environment files (never commit secrets)
.env
.env.local
.env.production

# Go compiled binaries
go-auth-service/server
go-gateway/server
java-service/server
*.exe

# Java / Maven
java-service/target/
*.class
*.jar

# Python
# Nested too - routers/__pycache__ slipped past the non-recursive pattern.
**/__pycache__/
**/*.pyc
python-ml-service/venv/
python-ml-service/.venv/

# OS
.DS_Store
Thumbs.db

# Local API-testing scratch files. login.json holds a plaintext password and
# cookies.txt a live session JWT; neither belongs in a public repo.
login.json
cookies.txt
test.csv

# Machine-local Claude Code config (memory exclusions, auto-memory toggle).
.claude/settings.local.json
