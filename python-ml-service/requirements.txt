fastapi==0.115.0
uvicorn[standard]==0.30.6
scikit-learn==1.5.2
pandas==2.2.3
numpy==1.26.4
pydantic==2.9.2
supabase==2.10.0
joblib==1.4.2
requests==2.32.3
# Required by FastAPI to parse multipart/form-data (File/Form params in dataset_router).
# FastAPI does not pull this in itself — without it the app fails at IMPORT time, not request time.
python-multipart==0.0.12
